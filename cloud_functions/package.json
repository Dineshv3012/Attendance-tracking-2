
{
  "name": "attendance_pro_functions",
  "version": "1.0.0",
  "engines": { "node": "20" },
  "scripts": {
    "build": "echo 'No build step'",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  }
}
